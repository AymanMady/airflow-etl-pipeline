# =============================================================================
#  Development dependencies (never deployed to production)
# =============================================================================
-r requirements.txt

pytest>=8.0,<9.0        # test framework (PHASE 11)
pytest-cov>=5.0,<7.0    # code coverage
ruff>=0.6,<1.0          # linter + formatter (replaces flake8 + black + isort)
